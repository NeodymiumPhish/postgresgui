import XCTest

final class PostgresGUIUITests: XCTestCase {
    override func setUpWithError() throws {
        continueAfterFailure = false
    }

    func testExample() throws {
        // UI tests placeholder
        // Will add actual UI tests later
    }
}
